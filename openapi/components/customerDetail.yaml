type: object
properties:
  id:
    type: integer
    format: number
  firstName:
    type: string
  lastName:
    type: string
  email:
    type: string
    format: email
  active:
    type: boolean
  phoneNumber:
    type: string
  avatar:
    type: string
    format: uri
  registeredOn:
    type: string
    format: date
  preferredStore:
    type: object
    properties:
      id:
        type: integer
        format: number
      phoneNumber:
        type: string
      storeManager:
        type: object
        properties:
          id:
            type: string
            format: number
          firstName:
            type: string
          lastName:
            type: string
          email:
            type: string
            format: email
          active:
            type: boolean
          phoneNumber:
            type: string
          avatar:
            type: string
            format: uri
          address:
            $ref: ./address.yaml
  address:
    $ref: ./address.yaml
