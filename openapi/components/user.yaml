type: object
properties:
  id:
    type: string
    format: integer
  firstName:
    type: string
  lastName:
    type: string
  email:
    type: string
    format: email
  customer:
    type: object
    properties:
      id:
        type: integer
        format: number
      totalRentals:
        type: integer
        format: number
  staff:
    type: object
    properties:
      id:
        type: integer
        format: number
      totalProcessedRentals:
        type: integer
        format: number
  storeManager:
    type: object
    properties:
      id:
        type: integer
        format: number
      totalProcessedRentals:
        type: integer
        format: number
      totalStaff:
        type: integer
        format: number
