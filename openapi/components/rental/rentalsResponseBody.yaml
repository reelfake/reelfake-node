type: object
properties:
  items:
    type: array
    items:
      properties:
        id:
          type: integer
        rentalStartDate:
          type: string
          format: date
        rentalEndDate:
          type: string
          format: date
        returnDate:
          type: string
          format: date
        rentalDuration:
          type: integer
        delayedByDays:
          type: integer
        amountPaid:
          type: number
          format: float
        discountAmount:
          type: number
          format: float
        paymentDate:
          type: string
          format: date
        rentalType:
          type: string
          enum: [in-store, online]
        storeId:
          type: integer
        movie:
          type: object
          properties:
            id:
              type: integer
            title:
              type: string
  length:
    type: integer
